[package]
name = "sql-translator"
version = "0.1.0"
edition = "2021"

[lib]
name = "sql_translator"
crate-type = ["lib", "cdylib"]

[dependencies]
sqlparser = { version = "0.61", features = ["serde"] }
serde_json = "1"

[dev-dependencies]
# none extra needed — sqlparser is enough for tests
